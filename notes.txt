Notas de correção: 

- Edição do funcionário: 
Padronizar tal como o arquivo edição_morador e edição_visitante.py, mesma ui, mesma lógica 

Listar Funcionários, helpers_funcionarios.py
- flash na tela de exibir funcionário, opção (4) 
- Escolher o campo que quer editar
- Criar a zona C. 10 vagas rotativas para os funcionários, 


- Exclusão do funcionário: 
Quando o mesmo é excluido (inativo), e tentamos inserir novamente no sistema, ele trava "O CPF já está cadastrado no sistema", porém não criamos nenhuma manipulação da tabela Funcionários inativos, Vejo duas opções criar toda essa manipulação, dando a opção de reativarmos aquele funcionário, ou facilitamos e excluimos para sempre, aqui estamos lidando com lixo de dados, certo???  


- Verificação GERAL CPF: 
Teremos essa função para rodar especificamente em cada coluna cpf de cada entidade ou uma função que dê o join na coluna cpf de todos verificando a unicidade do número? Ou ambas rodando no background? 

    def buscar_por_cpf(self, cpf: str) -> Funcionario:
        """Busca funcionário pelo CPF exato."""
        cursor = self._get_cursor()
        cursor.execute(queries.SELECT_FUNCIONARIO_BY_CPF, (cpf,))
        row = cursor.fetchone()
